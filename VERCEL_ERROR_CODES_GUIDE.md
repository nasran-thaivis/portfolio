# 🔧 คู่มือแก้ไข Vercel Error Codes

## Error Codes ที่พบบ่อยในโปรเจคนี้

### 1. FUNCTION_INVOCATION_FAILED (500)
**สาเหตุ:** Function หรือ API route มี runtime error

**วิธีแก้:**
- ตรวจสอบ Vercel Logs → Function Logs
- ตรวจสอบ Browser Console (F12) → Console tab
- ตรวจสอบว่า `NEXT_PUBLIC_API_URL` ตั้งค่าถูกต้อง
- ตรวจสอบว่า backend ทำงานที่ `https://portfolio-1-yxll.onrender.com`

**ตัวอย่าง Error:**
```
FUNCTION_INVOCATION_FAILED: Error in API route
```

**วิธีแก้ไข:**
1. ตรวจสอบ API routes ใน `src/app/api/`
2. ตรวจสอบว่า backend URL ถูกต้อง
3. ตรวจสอบ error handling ในโค้ด

---

### 2. FUNCTION_INVOCATION_TIMEOUT (504)
**สาเหตุ:** Function ใช้เวลานานเกินไป (เกิน 10 วินาที)

**วิธีแก้:**
- ตรวจสอบว่า backend ตอบสนองเร็วพอ
- เพิ่ม timeout handling ในโค้ด
- ตรวจสอบว่า backend service ไม่ sleep (Render free tier จะ sleep ถ้าไม่มีการใช้งาน)

**ตัวอย่าง Error:**
```
FUNCTION_INVOCATION_TIMEOUT: Function exceeded maximum duration
```

**วิธีแก้ไข:**
1. ตรวจสอบ backend logs ใน Render Dashboard
2. ถ้า backend sleep ให้รอสักครู่แล้วลองใหม่
3. พิจารณา upgrade Render plan หรือใช้ paid tier

---

### 3. NOT_FOUND (404)
**สาเหตุ:** Route หรือ resource ไม่พบ

**วิธีแก้:**
- ตรวจสอบว่า route path ถูกต้อง
- ตรวจสอบว่าไฟล์อยู่ในตำแหน่งที่ถูกต้อง
- ตรวจสอบว่า dynamic routes ทำงานถูกต้อง

**ตัวอย่าง Error:**
```
NOT_FOUND: Page not found
```

**วิธีแก้ไข:**
1. ตรวจสอบ route structure ใน `src/app/`
2. ตรวจสอบว่า dynamic routes (`[username]`) ทำงานถูกต้อง
3. ตรวจสอบว่า `not-found.tsx` มีอยู่

---

### 4. NO_RESPONSE_FROM_FUNCTION (502)
**สาเหตุ:** Function ไม่ตอบสนองหรือ crash

**วิธีแก้:**
- ตรวจสอบ Vercel Logs
- ตรวจสอบว่าโค้ดไม่มี infinite loop
- ตรวจสอบว่า environment variables ตั้งค่าถูกต้อง

**ตัวอย่าง Error:**
```
NO_RESPONSE_FROM_FUNCTION: Function did not respond
```

**วิธีแก้ไข:**
1. ตรวจสอบ Vercel Function Logs
2. ตรวจสอบว่าไม่มี infinite loop ในโค้ด
3. ตรวจสอบว่า environment variables ตั้งค่าถูกต้อง

---

### 5. ROUTER_CANNOT_MATCH (502)
**สาเหตุ:** Router ไม่สามารถ match route ได้

**วิธีแก้:**
- ตรวจสอบว่า route structure ถูกต้อง
- ตรวจสอบว่า dynamic routes ทำงานถูกต้อง
- ตรวจสอบว่าไม่มี conflicting routes

**ตัวอย่าง Error:**
```
ROUTER_CANNOT_MATCH: Cannot match route
```

**วิธีแก้ไข:**
1. ตรวจสอบ route structure ใน `src/app/`
2. ตรวจสอบว่า dynamic routes (`[username]`) ทำงานถูกต้อง
3. ตรวจสอบว่าไม่มี conflicting routes

---

### 6. DEPLOYMENT_NOT_FOUND (404)
**สาเหตุ:** Deployment ไม่พบหรือถูกลบ

**วิธีแก้:**
- ตรวจสอบว่า deployment ยังอยู่
- ตรวจสอบว่า project ยังเชื่อมต่อกับ Git repository

**ตัวอย่าง Error:**
```
DEPLOYMENT_NOT_FOUND: Deployment not found
```

**วิธีแก้ไข:**
1. ตรวจสอบ Vercel Dashboard → Deployments
2. ตรวจสอบว่า project ยังเชื่อมต่อกับ Git repository
3. สร้าง deployment ใหม่ถ้าจำเป็น

---

## 🔍 วิธีตรวจสอบ Error

### 1. ตรวจสอบ Vercel Logs
1. ไปที่ Vercel Dashboard
2. เลือก Project ของคุณ
3. ไปที่ **Deployments** tab
4. เลือก deployment ล่าสุด
5. คลิก **View Function Logs** หรือ **View Build Logs**

### 2. ตรวจสอบ Browser Console
1. เปิดเว็บที่ deploy แล้ว
2. เปิด Browser Console (F12)
3. ดู **Console** tab → หา error messages
4. ดู **Network** tab → ตรวจสอบ API calls

### 3. ตรวจสอบ Build Logs
1. ไปที่ Vercel Dashboard → Deployments
2. เลือก deployment ล่าสุด
3. คลิก **View Build Logs**
4. หา error messages ใน build process

---

## 🛠️ วิธีแก้ไข Error ทั่วไป

### 1. Environment Variables ไม่ถูกต้อง
**อาการ:** Function ไม่ทำงานหรือใช้ค่า default

**วิธีแก้:**
1. ไปที่ Vercel Dashboard → Project Settings → Environment Variables
2. ตรวจสอบว่า:
   - `NEXT_PUBLIC_API_URL` = `https://portfolio-1-yxll.onrender.com`
   - `NEXT_PUBLIC_SITE_URL` = URL ของ frontend (แนะนำ)
3. Redeploy project

### 2. Backend ไม่ตอบสนอง
**อาการ:** API calls timeout หรือ fail

**วิธีแก้:**
1. ตรวจสอบว่า backend ทำงานที่ `https://portfolio-1-yxll.onrender.com`
2. ตรวจสอบ Render Dashboard → Web Service → Logs
3. ถ้า backend sleep (Render free tier) ให้รอสักครู่แล้วลองใหม่

### 3. CORS Error
**อาการ:** Browser console แสดง CORS error

**วิธีแก้:**
1. ไปที่ Render Dashboard → Web Service → Environment Variables
2. เพิ่ม/แก้ไข `CORS_ORIGIN`:
   ```
   CORS_ORIGIN=https://your-app.vercel.app,http://localhost:3000
   ```
3. Redeploy backend

### 4. Build Fails
**อาการ:** Deployment fail ใน build step

**วิธีแก้:**
1. ทดสอบ build locally: `npm run build`
2. ตรวจสอบ build logs ใน Vercel Dashboard
3. ตรวจสอบว่า dependencies ครบถ้วน
4. ตรวจสอบว่า TypeScript/JavaScript syntax ถูกต้อง

---

## 📋 Checklist สำหรับแก้ไข Error

- [ ] ตรวจสอบ Vercel Logs (Function Logs และ Build Logs)
- [ ] ตรวจสอบ Browser Console (F12)
- [ ] ตรวจสอบ Environment Variables ใน Vercel Dashboard
- [ ] ตรวจสอบว่า backend ทำงานที่ `https://portfolio-1-yxll.onrender.com`
- [ ] ตรวจสอบ CORS settings ใน backend
- [ ] ทดสอบ build locally: `npm run build`
- [ ] ตรวจสอบว่า route structure ถูกต้อง
- [ ] ตรวจสอบว่าไม่มี infinite loop ในโค้ด

---

## 🚀 Quick Fixes

### ถ้าเจอ FUNCTION_INVOCATION_FAILED:
1. ตรวจสอบ Vercel Function Logs
2. ตรวจสอบว่า `NEXT_PUBLIC_API_URL` ตั้งค่าถูกต้อง
3. ตรวจสอบว่า backend ทำงาน

### ถ้าเจอ FUNCTION_INVOCATION_TIMEOUT:
1. ตรวจสอบว่า backend ตอบสนองเร็วพอ
2. ตรวจสอบว่า backend ไม่ sleep
3. พิจารณา upgrade Render plan

### ถ้าเจอ NOT_FOUND:
1. ตรวจสอบ route structure
2. ตรวจสอบว่า dynamic routes ทำงานถูกต้อง
3. ตรวจสอบว่าไฟล์อยู่ในตำแหน่งที่ถูกต้อง

---

## 📞 ติดต่อ Support

ถ้ายังแก้ไขไม่ได้:
1. ตรวจสอบ Vercel Documentation: https://vercel.com/docs
2. ติดต่อ Vercel Support: https://vercel.com/support
3. ตรวจสอบ GitHub Issues หรือ Community Forums


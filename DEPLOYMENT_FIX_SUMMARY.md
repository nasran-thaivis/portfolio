# 🔧 สรุปการแก้ไขปัญหา Deployment

## ❌ ปัญหาที่พบ

หลังจากตั้งค่า `NEXT_PUBLIC_API_URL` ใน Vercel แล้ว แต่หน้าเว็บยังไม่แสดง

## 🔍 สาเหตุ

1. **`getBaseUrl()` ไม่รองรับ Vercel Production**
   - ใน server-side rendering, ใช้ `process.env.NEXT_PUBLIC_SITE_URL`
   - ถ้าไม่ได้ตั้งค่า จะ fallback เป็น `http://localhost:3000` ซึ่งไม่ถูกต้อง
   - Vercel มี `VERCEL_URL` ที่ตั้งค่าอัตโนมัติ แต่ไม่ได้ใช้

2. **Environment Variables ที่ต้องตั้งค่า:**
   - `NEXT_PUBLIC_API_URL` ✅ (ตั้งค่าแล้ว)
   - `NEXT_PUBLIC_SITE_URL` ⚠️ (อาจไม่ได้ตั้งค่า)

## ✅ การแก้ไข

### 1. สร้าง Utility Function ใหม่

สร้างไฟล์ `src/lib/getBaseUrl.ts` ที่:
- ใช้ `process.env.VERCEL_URL` ใน Vercel production (อัตโนมัติ)
- Fallback ไปที่ `NEXT_PUBLIC_SITE_URL` ถ้ามี
- ใช้ `https://` protocol ใน production

### 2. อัปเดตไฟล์ทั้งหมด

แก้ไขไฟล์ต่อไปนี้ให้ใช้ utility function เดียวกัน:
- ✅ `src/app/[username]/page.tsx`
- ✅ `src/app/[username]/about/page.jsx`
- ✅ `src/app/[username]/portfolio/page.jsx`
- ✅ `src/app/[username]/review/page.jsx`
- ✅ `src/app/[username]/contact/page.jsx`

## 📋 ขั้นตอนต่อไป

### 1. Commit และ Push การเปลี่ยนแปลง

```bash
git add .
git commit -m "Fix getBaseUrl() to support Vercel production"
git push
```

### 2. Vercel จะ Auto-Deploy

- Vercel จะ detect การเปลี่ยนแปลงและ deploy อัตโนมัติ
- รอให้ deploy เสร็จ (ประมาณ 2-5 นาที)

### 3. ตรวจสอบ Environment Variables ใน Vercel

ไปที่ **Vercel Dashboard** → **Project Settings** → **Environment Variables**

ตรวจสอบว่ามี:
- ✅ `NEXT_PUBLIC_API_URL` = `https://portfolio-1-yxll.onrender.com`
- ⚠️ `NEXT_PUBLIC_SITE_URL` = `https://your-app.vercel.app` (แนะนำให้ตั้งค่า)

**หมายเหตุ:** `VERCEL_URL` จะถูกตั้งค่าอัตโนมัติโดย Vercel ไม่ต้องตั้งค่าด้วยตัวเอง

### 4. ทดสอบ

หลังจาก deploy เสร็จ:
1. เปิดเว็บที่ deploy แล้ว
2. เปิด Browser Console (F12)
3. ตรวจสอบว่าไม่มี error
4. ทดสอบการโหลดหน้าเว็บ

## 🔍 วิธีตรวจสอบว่าแก้ไขสำเร็จ

### ใน Browser Console:

```javascript
// ตรวจสอบว่า getBaseUrl() ทำงานถูกต้อง
console.log(window.location.origin)
// ควรแสดง URL ของเว็บที่ deploy แล้ว
```

### ใน Vercel Logs:

1. ไปที่ **Deployments** → เลือก deployment ล่าสุด
2. คลิก **View Function Logs**
3. ตรวจสอบว่าไม่มี error เกี่ยวกับ `getBaseUrl()` หรือ `localhost:3000`

## 📝 หมายเหตุ

- ✅ `VERCEL_URL` จะถูกตั้งค่าอัตโนมัติโดย Vercel ในทุก deployment
- ✅ ไม่ต้องตั้งค่า `VERCEL_URL` ด้วยตัวเอง
- ⚠️ แนะนำให้ตั้งค่า `NEXT_PUBLIC_SITE_URL` เพื่อความชัดเจน (แต่ไม่จำเป็น)

## 🚀 ถ้ายังมีปัญหา

1. **ตรวจสอบ Vercel Logs:**
   - ไปที่ Deployments → View Function Logs
   - หา error messages

2. **ตรวจสอบ Browser Console:**
   - เปิด Browser Console (F12)
   - ดู Console และ Network tabs

3. **ตรวจสอบ Backend:**
   ```bash
   curl https://portfolio-1-yxll.onrender.com/api/hero-section
   ```

4. **ตรวจสอบ CORS:**
   - ดู Network tab ใน Browser Console
   - ตรวจสอบว่าไม่มี CORS error


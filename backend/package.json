{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "sh -c \"if [ -z \\\"$DATABASE_URL\\\" ]; then echo '❌ ERROR: DATABASE_URL is not set!' && echo 'Please set DATABASE_URL environment variable in Render Dashboard' && exit 1; fi && MASKED_URL=$(echo $DATABASE_URL | sed 's/:[^:@]*@/:****@/') && echo '📝 DATABASE_URL is set: $MASKED_URL' && HOST=$(echo $DATABASE_URL | sed -n 's/.*@\\([^:]*\\).*/\\1/p') && if [ -n \\\"$HOST\\\" ] && echo \\\"$HOST\\\" | grep -qE '^(localhost|127\\.0\\.0\\.1)$'; then echo '⚠️  WARNING: DATABASE_URL host is localhost or 127.0.0.1!' && echo 'This will not work in production. Please use Render PostgreSQL database URL.' && echo 'Go to Render Dashboard → Your PostgreSQL Service → Copy Internal Database URL' && exit 1; fi && echo '🔧 Generating Prisma Client...' && npx prisma generate && echo '📦 Running database migrations...' && npx prisma migrate deploy && echo '🚀 Starting application...' && node dist/main\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prisma:seed": "ts-node prisma/seed.ts",
    "validate:env": "node scripts/validate-env.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.937.0",
    "@aws-sdk/s3-request-presigner": "^3.937.0",
    "@nestjs/common": "^11.1.9",
    "@nestjs/core": "^11.1.9",
    "@nestjs/mapped-types": "*",
    "@nestjs/platform-express": "^11.1.9",
    "@prisma/client": "^7.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.3",
    "dotenv": "^17.2.3",
    "prisma": "^7.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "@types/express": "^5.0.5",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.10.1",
    "@types/uuid": "^8.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
